Focused playlist correction regression
======================================

Implementation under test
-------------------------

- Commit: 9d54da466dbcb3d92e530c41da36bf5314e0bb14
- Fresh AVD: nextplayer_playlist_qa_api37_regression
- Serial: emulator-5556 (targeted explicitly for every device operation)
- Runtime: API 37, ARM64 v8a, Pixel 6, Google APIs 16 KB image

Connected instrumentation
-------------------------

- Command: ANDROID_SERIAL=emulator-5556 ./gradlew :feature:playlist:connectedDebugAndroidTest --console=plain
- Result: BUILD SUCCESSFUL; 10 tests, 10 passed, 0 failures, 0 errors, 0 skipped.
- XML: regression-01-instrumentation-results.xml
- XML timestamp: 2026-07-15T14:59:28; elapsed test time: 23.326 seconds.
- PlaylistDetailScreenTest: 8/8 passed.
- PlaylistListScreenTest: 2/2 passed.
- deleteRequiresConfirmationBeforeDispatchingAction passed in 2.866 seconds.

Manual correction journey
-------------------------

- Clean app data was launched after installing the current debug APK.
- Two valid four-second H.264/MP4 videos were media-scanned into Movies.
- Created editable playlist `QA Regression` and added both selected videos through the Add to playlist chooser.
- The visible delete dialog showed the exact message `Remove "QA Regression"?` before any delete action (`regression-08-quoted-delete-confirmation.png`). The dialog was cancelled for the playback journey.
- Tapping the second item launched PlayerActivity with `content://media/external/video/media/20` (`regression-13-playback-activity.txt`).
- The media session reported `playlist-regression-two.mp4` and queue size 2 (`regression-14-playback-media-session.txt`).
- The media notification reported `android.largeIcon=Icon (Icon(typ=BITMAP size=126x94))` (`regression-17-playback-notification.txt`). The expanded media card visibly rendered that generated artwork (`regression-18-media-notification.png`).
- The retained full playback log has zero `files/thumbnails` references, zero app-private `file:///data/user/0/dev.anilbeesetti.nextplayer.debug` references, and zero `EACCES` entries. Consequently it contains no SystemUI/app `Permission denied` involving `files/thumbnails`.
- Android framework emitted unrelated `ContentProviderHelper` warnings containing `assuming permission denied` while holding the window-manager lock. They do not reference SystemUI, Next Player, thumbnails, or an app-private URI.
- The API 37 goldfish MediaCodec failed its binder/memfd AVC input path. This emulator decoder limitation occurred after the selected URI, two-item queue, notification metadata, and bitmap artwork were established; it is not a playlist-routing or thumbnail-permission failure.
- The editable playlist was finally deleted through its confirmation dialog and the empty Playlists state returned (`regression-19-deleted-empty-list.png`).
- The crash buffer was empty (`regression-21-crash-buffer.txt`, zero lines).

Artifact integrity and cleanup
------------------------------

- Every retained regression XML file validates with `xmllint --noout` and has no UI Automator suffix.
- PRE and POST CLI AVD inventories compare byte-for-byte equal.
- PRE and POST raw AVD inventories compare byte-for-byte equal.
- PRE and POST adb inventories compare byte-for-byte equal.
- Only `nextplayer_playlist_qa_api37_regression` was stopped and deleted; its `.ini` and `.avd` paths are absent.
- The pre-existing `Pixel_6a` at `emulator-5554` remained present and was never targeted.
